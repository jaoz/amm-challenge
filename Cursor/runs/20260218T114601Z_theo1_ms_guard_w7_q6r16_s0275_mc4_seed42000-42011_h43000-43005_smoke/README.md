# Smoke run

Short smoke test of the multi-seed local optimizer pipeline and Cursor layout (workers, shared tools, seed-offset logic). After collect, the run result is `best.sol` (same as `best_overall.sol`).
