## Run: 20260218T120953Z_theo1_speed_hw_w14_q30r60_md0001

### Goal
- Hardware-aware speed optimization run using higher per-candidate simulation fidelity and relaxed acceptance threshold.

### Base / champion used
- Base strategy file (copied): `Cursor/runs/20260218T120953Z_theo1_speed_hw_w14_q30r60_md0001/base.sol`
- Champion snapshot source: `Cursor/strategies/current/theo1_current.sol`

### Seeds
- Train (eval): `42000..42007` (8 seeds)
- Holdout: `43000..43003` (4 seeds)
- Per-seed diversity: `seed_offset = eval_seed * 1_000_000`, per-sim seed `seed = seed_offset + i`.

### Key optimizer settings
- `workers=4` default launch target, clamped to safe range (`max-safe-workers=6`)
- `sim_workers=1` to avoid nested worker oversubscription
- `quick_sims=30`, `refine_sims=60`
- `min_delta_edge=0.001`
- Focused mutable constants for faster search over high-signal dimensions

### How to start / restart (exact command)
From repo root:

```powershell
py -3.10 "Cursor\runs\20260218T120953Z_theo1_speed_hw_w14_q30r60_md0001\start.py"
```

