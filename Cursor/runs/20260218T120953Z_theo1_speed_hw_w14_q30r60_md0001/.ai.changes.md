## Changes for run: 20260218T120953Z_theo1_speed_hw_w14_q30r60_md0001

- 20260218T120953Z: Created new run folder and copied base strategy from `Cursor/strategies/current/theo1_current.sol`.
- 20260218T120953Z: Configured speed-oriented launch (`workers=14`, `quick_sims=30`, `refine_sims=60`, `min_delta_edge=0.001`) with expanded train/holdout seed sets.
- 20260218T120953Z: Narrowed mutable constants to a focused high-signal subset to improve effective search speed on current hardware.
- 20260218T121839Z: Launched run via this folder’s `start.ps1`; manifest created with `hours=1.5`, `workers=14`, `quick_sims=30`, `refine_sims=60`, `min_delta_edge=0.001`.
- 20260218T121839Z: Attempted collect snapshot multiple times; no `workers/worker_*/best.json` produced yet, so `best_overall.sol` / `best.sol` are pending until first worker checkpoint.
- 20260218T124719Z: Updated launcher flow to use detached Python entrypoint `start.py` (independent process) instead of long-running PowerShell launch chain.
- 20260218T124719Z: Added run-level logs (`run.log`, `launcher.log`) and constrained default runtime concurrency (`workers=4`, `sim_workers=1`, safe cap 6, staggered spawn) to reduce OOM risk in Cursor.
- 20260218T124807Z: Added run-local `start.py` launcher and changed `start.ps1` to delegate to it; launcher now starts optimization with a detached Python process so IDE shell exits immediately.
- 20260218T124807Z: Hardened optimizer launch path with `max-safe-workers`, `spawn-stagger-seconds`, and reduced terminal output from `launch` mode to minimize IDE memory pressure.
- 20260218T124935Z: Fixed detached Windows process stdio inheritance (`close_fds=False`) in both worker spawner and run launcher to avoid silent launch/logging failures.
- 20260218T125103Z: Reworked `start.py` to launch detached worker processes directly (`--mode worker`) and write `manifest.json` itself; no dependency on nested launch subprocess chain.
- 20260218T125103Z: Confirmed run relaunch with OOM-safe settings (`workers=4`, `sim_workers=1`, staggered spawn) and run-level logs in `run.log` + `launcher.log`.
- 20260218T130216Z: Added stricter spawn safety: `CREATE_NO_WINDOW` on Windows, memory-based worker clamping (`THEO1_PER_WORKER_MEM_GB`, `THEO1_MIN_FREE_MEM_GB`), and immediate spawn health check (`worker_spawn_failed` logging).
- 20260218T130216Z: Relaxed minimum worker floor from 3 to 1 so launcher can auto-downshift to avoid OOM instead of overcommitting RAM.
- 20260218T130327Z: Switched worker interpreter on Windows to `pythonw.exe` (when available) in both launch paths to eliminate remaining console window popups.

